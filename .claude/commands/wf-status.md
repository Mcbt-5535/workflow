---
description: Show all plans + their progress at a glance.
---

Show plan status:

1. If `.claude/workflow/plans/STATUS.md` exists (auto-generated by the PostToolUse hook), read and print it verbatim. It's already a dashboard.
2. Otherwise, fall back: list every `.claude/workflow/plans/*.md` (skip README.md and STATUS.md), parse frontmatter `status`, count `- [x]` / `- [ ]` / `- [?]`, and print:

```
| Plan | Status | Progress | Blocked |
|---|---|---|---|
| 2026-05-22-add-cli-flag | in_progress | 3/5 | 0 |
| 2026-05-20-refactor-db | done | 7/7 | 0 |
```

3. Below the output, list any `HUMAN:` items from active plans (one bullet each, with plan path).
4. No prose. Just the dashboard / table + list.

